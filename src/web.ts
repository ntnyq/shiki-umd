/**
 * Web bundle of Shiki, with web-related languages (HTML, CSS, JS, TS, JSON,
 * Markdown, Vue, JSX, etc.) and all themes. Much smaller than the full bundle.
 *
 * Exposed as the `Shiki` global in the UMD build.
 */
export * from 'shiki/bundle/web'
